import wollok.game.*

import juego.*
import elementos.*
import mapas.*
import contador.*
import logicaDeNiveles.*

program snowBros {
	
	juego.prepararPresentacion()
	keyboard.s().onPressDo {
		if(!juego.juegoIniciado()){
			game.removeVisual(imagenInicial)
			juego.iniciarJuego()
			juego.juegoIniciado(true) 
		}
	}
	
	
	
	game.start()
}